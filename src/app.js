import updateCatFact from './dom.js';

const initApp = async () => {
  // Tutaj możesz zainicjować pobieranie danych i aktualizację DOM
  // Na przykład:
  updateCatFact();

  // Możesz także dodać więcej logiki inicjalizacyjnej, jeśli jest potrzebna
};

initApp();
